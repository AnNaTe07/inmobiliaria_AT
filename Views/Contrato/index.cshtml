@model IEnumerable<inmobiliaria_AT.Models.Contrato>

@{
    ViewData["Title"] = "Contratos";

}
<div class="text-center">
    <h1 class="display-4">Listado de contratos activos</h1>
</div>
@*<div class="filtro_cont">
    <div class="titulo_filtro">
        <label for="tipoFiltro">Filtrar:</label>
    </div>
    <div class="filtro_tipo">
        <select id="tipoFiltro" onchange="filtrar()">
            <option value="0">Seleccionar</option>
            <option value="Propietario">Propietario</option>
            <option value="Propiedad">Inmueble</option>
        </select>
    </div>
    <div class="filtro_opciom">
        <div id="filtroPropietario" class="form-group col-md-2 form-group-spacing" style="display:none;">
            <select class="form-control" asp-items="ViewBag.Propietarios"></select>
        </div>
        <div id="filtroPropiedad" class="form-group col-md-2 form-group-spacing" style="display:none;">
            <select class="form-control" asp-items="ViewBag.Inmuebles"></select>
        </div>
    </div>
</div>*@
<form method="get" action="@Url.Action("Index")">
    <div class="filtro_cont row align-items-end">
        <!-- filtro por tipo (Propietario o Inmueble) -->
        <div class="col-md-3">
            <label for="tipoFiltro" class="form-label">Filtrar por:</label>
            <select id="tipoFiltro" class="form-select" onchange="filtrar()">
                <option value="0">Todos los contratos</option>
                <option value="Propietario">Propietario</option>
                <option value="Propiedad">Inmueble</option>
            </select>
        </div>

        <!-- filtro por propietario -->
        <div id="filtroPropietario" class="col-md-3" style="display:none;">
            <label for="propietarioId" class="form-label">Seleccionar Propietario:</label>
            <select name="propietarioId" class="form-select" asp-items="ViewBag.Propietarios">
                <option value="">Seleccionar Propietario</option>
            </select>
        </div>

        <!--filtro por inmueble -->
        <div id="filtroPropiedad" class="col-md-3" style="display:none;">
            <label for="inmuebleId" class="form-label">Seleccionar Inmueble:</label>
            <select name="inmuebleId" class="form-select" asp-items="ViewBag.Inmuebles">
                <option value="">Seleccionar Inmueble</option>
            </select>
        </div>

        <!-- Boton de filtro -->
        <div class="col-md-2">
            <button type="submit" class="btn btn-primary">Filtrar</button>
        </div>
    </div>
</form>


<table class="table">
    <thead>
        <th>Código</th>

        <th>
            Dirección

        </th>
        <th>
            Propietario
        </th>
        <th>
            Inquilino

        </th>

        <th>
            @Html.DisplayNameFor(model => model.Tipo)

        </th>
        <th>
            @Html.DisplayNameFor(model => model.Plazo)

        </th>


    </thead>

    <tbody>
        @if (Model == null || !Model.Any())
        {
            <tr>
                <td colspan="3" class="text-center">No se encontraron contratos.</td>
            </tr>
        }
        else
        {

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Id)

                    </td>

                    <td>

                        @(item.Inmu.Direccion)
                    </td>

                    <td>
                        @(item.Prop.NombreCompleto)
                    </td>

                    <td>
                        @(item.Inqui.NombreCompleto)

                    </td>

                    <td>
                        @Html.DisplayFor(modelItem => item.Tipo)

                    </td>

                    <td>
                        @Html.DisplayFor(modelItem => item.Plazo)

                    </td>
                    <td></td>

                    @*--------ICONOS PARA EDITAR, DETALLE Y ELIMINAR -------------*@
                    <td>
                        

                        <a href="/Contrato/Detalle/@item.Id"></a>
                        <form action="@Url.Action("Detalle", new{id=item.Id})" method="get" style="display: inline;">
                            <button type="submit" class="btn btn-info btn-custom" title="Detalles">
                                <i class="fa-solid fa-circle-info"></i>
                            </button>
                        </form> @* AGREGAR @item.Tipo *@
                        <button type="submit" class="btn btn-danger btn-custom"
                            onclick="eliminar(@item.Id, '@item.Inmu.Direccion', '@item.Prop.NombreCompleto', '@item.Inqui.NombreCompleto', '@item.Tipo', '@item.Plazo')"
                            title="Eliminar">
                            <i class="fas fa-trash"></i>
                        </button>


                        <button type="button" class="btn btn-success btn-custom" data-bs-toggle="modal"
                            data-bs-target="#paymentModal" data-id="@item.Id" title="Comprar">
                            <h7 style="font-weight: bold;">Pagar</h7>
                        </button>

                        <form action="@Url.Action("ListadoPagos","Pago", new{id=item.Id})" method="get"
                            style="display: inline;">
                            <button type="submit" class="btn btn-warning btn-custom" title="Listado de pagos">
                                <i class="fa-solid fa-dollar-sign"></i>
                            </button>
                        </form>

                    </td>
                </tr>
            }
        }
        <td>
            <button type="button" class="btn btn-ligth">
                <a href="/Contrato/Editar" title="Agregar Contrato">
                    <i class="fa-duotone fa-solid fa-plus"></i>
                </a>
            </button>
            </tr>




    </tbody>

</table>

<div class="modal fade" id="modal_eliminar_contrato" tabindex="-1" aria-labelledby="modal_eliminar_contratoLabel"
    aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modal_eliminar_contratoLabel">Confirmar Eliminación</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form method="post" asp-action="Eliminar" id="form_eliminar_contrato">
                <div class="modal-body">
                    <input type="hidden" name="Id" value="" id="contrato_eliminar_id">
                    <p>
                        <span id="contrato_eliminar_direccion"></span><br>
                        <span id="contrato_eliminar_propietario"></span><br>
                        <span id="contrato_eliminar_inquilino"></span><br>
                        <span id="contrato_eliminar_tipo"></span><br>
                        <span id="contrato_eliminar_plazo"></span>
                    </p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="submit" class="btn btn-danger"> Confirmar</i></button>
                </div>
            </form>
        </div>
    </div>
</div>

@if (TempData["SuccessMessage"] != null)
{
    <div class="modal fade" id="modal_success_message" tabindex="-1" aria-labelledby="modalSuccessMessageLabel"
        aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p>@TempData["SuccessMessage"]</p>
                </div>
            </div>
        </div>
    </div>
}
@Html.Partial("../Pago/Nuevo", new inmobiliaria_AT.Models.Pago())

@section Scripts {
    <script>
        $(document).ready(function () {
            // Muestro el modal de éxito si existe un mensaje en TempData
            if ($('#modal_success_message').length) {
                $('#modal_success_message').modal('show');
                // Oculto el modal después de 3 segundos
                setTimeout(function () {
                    $('#modal_success_message').modal('hide');
                }, 3000);
            }
        });

        function eliminar(id, direccion, propietario, inquilino, tipo, plazo) {
            document.querySelector("#contrato_eliminar_id").value = id;
            document.querySelector("#contrato_eliminar_direccion").innerText = "Dirección: " + direccion;
            document.querySelector("#contrato_eliminar_propietario").innerText = "Propietario: " + propietario;
            document.querySelector("#contrato_eliminar_inquilino").innerText = "Inquilino: " + inquilino;
            document.querySelector("#contrato_eliminar_tipo").innerText = "Tipo: " + tipo;
            document.querySelector("#contrato_eliminar_plazo").innerText = "Plazo: " + plazo;
            $("#modal_eliminar_contrato").modal("show");
        }

        function filtrar() {
            var tipoFiltro = document.getElementById("tipoFiltro").value;
            var filtroPropietario = document.getElementById("filtroPropietario");
            var filtroPropiedad = document.getElementById("filtroPropiedad");

            if (tipoFiltro === "Propietario") {
                filtroPropietario.style.display = "block";
                filtroPropiedad.style.display = "none";
            } else if (tipoFiltro === "Propiedad") {
                filtroPropietario.style.display = "none";
                filtroPropiedad.style.display = "block";
            } else {
                filtroPropietario.style.display = "none";
                filtroPropiedad.style.display = "none";
            }
        }
        

    </script>
    <link rel="stylesheet" type="text/css" href="../css/contrato.css">
    <script src="../js/nuevoPago.js"></script>

}